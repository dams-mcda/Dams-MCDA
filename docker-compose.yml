version: "2"

services:
  shiny_server:
    build: .
    ports:
      - "3838:3838"
    volumes:
      - ./src:/srv/shiny-server/
      - ./logs:/var/log/shiny-server/
    user: 'shiny'
    restart: always
